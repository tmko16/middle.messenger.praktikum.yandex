<div class="profile-form">
    {{#each profile.fields}}
    <div class="profile-form__field">
        <div class="profile-form__label">
            {{ this.title }}
        </div>
        <input class="profile-form__input" name="{{ this.name }}" placeholder="{{ this.value }}" />
    </div>
    {{/each}}
</div>